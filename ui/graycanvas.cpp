#include "graycanvas.h"
#include <cmath>

uint8_t GrayCanvas::floatToInt(float intensity) {
    // [TODO]: Task 3 -- convert a float into a uint8_t

    return 0;
}

void GrayCanvas::initializeGrayCanvas() {
    // [TODO]: Task 4 -- initialize a 10 x 10 gray canvas where each pixel has an
    //          intensity of 0.123

    // [TODO]: Task 5 -- call the createHeart() function to display the canvas with
    //          the white heart
}

void GrayCanvas::createHeart() {
    // [TODO]: Task 5 -- create a heart on the canvas by turning certain pixels white
    //          so it matches the canvas in the handout
}
